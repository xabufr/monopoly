project(monopoly)
include_directories(src/)
file(GLOB_RECURSE
		source_files
		src/*)

add_executable(
		monopoly
		${source_files}
		)
target_link_libraries(
		monopoly
		sfml-system
		sfml-window
		sfml-graphics
		sfml-audio
		)
